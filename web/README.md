# Web UI (Astro)

Minimal Astro frontend for the LinkedIn Trend Posts Generator.

## Development

```bash
npm install
npm run dev
```

By default the UI expects the API at `http://localhost:8000`.
Set `PUBLIC_API_BASE` if your API runs elsewhere.

## Build

```bash
npm run build
npm run preview
```
